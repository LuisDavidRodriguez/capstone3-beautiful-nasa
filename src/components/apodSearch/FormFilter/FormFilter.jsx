import React, { useState } from 'react';

const FormFilter = () => {
  const [ state, setState ] = useState({});

  return (
    <form>
      <input>
      
      </input>
    </form>
  );
};

export default FormFilter;
