import React from 'react';
import style from './front.module.scss';

const Front = () => (
  <section className={style.container}>
    <h3 className={style.title}>Hi there Human!</h3>
  </section>
);

export default Front;
